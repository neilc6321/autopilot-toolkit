---
Status: resolved
---

# 03-orchestrator-reasonix-protocol

Adapt the autopilot-orchestrator SKILL.md for reasonix: replace opencode `task` dispatch with `run_skill`, replace `edit`/`write`/`read` with `edit_file`/`write_file`/`read_file`, and adapt GitHub integration to MCP-priority + `bash gh` fallback.

## Comments
